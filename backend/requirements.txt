fastapi>=0.118
uvicorn[standard]>=0.34
sqlalchemy>=2.0.36
pymysql>=1.1.1
python-dotenv>=1.0.1
pydantic>=2.10
openai>=1.60
sqlglot>=26.0
numpy>=1.26
scipy>=1.11        # hypothesis tests (t-test, Mann-Whitney U, Shapiro-Wilk)
statsmodels>=0.14  # ANOVA via OLS + sm.stats.anova_lm

# Test deps (also covers the root-level tests/ suite)
pytest>=8.3
pytest-cov>=5.0      # coverage reporting (pytest --cov)
httpx>=0.27          # fastapi.testclient transport
pandas>=2.2          # cleaner tests
beautifulsoup4>=4.12 # crawl.py imports — needed at test-collection time
