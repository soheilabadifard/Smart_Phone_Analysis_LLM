"""ETL pipeline package — extract (crawl), transform (cleaning), load (DB).

Order of operations is documented in `pipeline.py` at the repo root, which
shells out to each module here in sequence.
"""
